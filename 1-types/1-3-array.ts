{
    // Array
    const aa: string[] = ['a', 'b', 'c']
    const bb: Array<string> = ['a', 'b', 'c'] // 비추

    // Tuple -> 배열인데 서로 다른 타입을 가질 수 있는 배열이다. -> (사용을 권장하지 않음)
    const student: [string, number] = ['a', 123]
}